# app-store.yaml — App Store Connect state for MadFrog VPN iOS.
#
# Re-verify via ASC API before claiming anything changed. Don't trust
# this file blindly — see playbooks/asc-api-checks.md for a 5-line
# bash snippet to re-fetch state.
#
# ⚠️ 2026-07-11: the `versions:` block below is STALE (last real ASC pull was
# ~2026-06-04) and was drifting badly — it still showed 1.0.28/91 as the live
# public build. Cross-validated 2026-07-11 via (a) a separate audit's direct
# ASC API check and (b) production telemetry (app_events.app_version values
# actually seen from real devices: "1.0.27 (90)", "1.0.32 (113)",
# "1.0.33 (123)") that **the real live public build is 1.0.32 (113)**, with
# 1.0.33 (123) on TestFlight. `current` below is corrected to match; the
# detailed ASC version/build UUIDs under it were NOT re-fetched this session
# (didn't want to invent IDs) — treat those specific IDs as unverified until
# a fresh ASC API pull. See docs/audits/2026-07-11-production-stability-audit.md
# and docs/state/project.yaml (2026-06-29, closer to current than this file was).
#
# ⚠️ 2026-07-11 (later same day): 1.0.33 was re-verified on-device-equivalent
# via iOS Simulator against build 123's exact source (log-confirmed) ahead of
# the planned App Store submission — see docs/audits/ for the checklist pass.
# Found + fixed one real bug: Poland/WAW exit rendered as raw "pl" (no flag/
# name) in the server picker — `ConfigStore.swift`'s `countryDisplay` dict was
# missing a "pl" entry (client-only fix, `backend/clientconfig.go` was already
# correct). Cut as **build 124** (same 1.0.33 train, not yet submitted to
# review), archived+uploaded via `ios-archive-upload` CI (tag `ios-build-124`),
# VALID in ASC, assigned to Internal Testers (verified via include=betaGroups).
# — 123 is superseded (still has the pl-flag bug, don't test against it).
#
# ⚠️ 2026-07-11 (still later same day): a Fable-model code-quality audit of
# the whole iOS client (theme-system duplication, AppState.swift god-object,
# 2 more verified bugs) was implemented + verified the same day — see
# roadmap.yaml#now.audit_2026_07_11_fable_code_quality for the full list.
# Cut as **build 125** (same 1.0.33 train), VALID in ASC, assigned to
# Internal Testers. **1.0.33/125 is now the build to test on-device before
# App Store submission** — 124 is superseded (same base, missing this batch).
#
# ⚠️ 2026-07-11 (even later same day): on-device test of 125 on a real iPhone
# surfaced a real bug — a device's local config cache got poisoned by a
# garbled/non-JSON response somewhere on its network path (backend confirmed
# healthy via a direct live fetch), and every subsequent connect then failed
# inside the tunnel with sing-box's own `decode config` JSON errors, with the
# server picker silently showing zero countries. Root cause:
# `AppState.isUsableConfigPayload` only did a `contains("\"outbounds\"")`
# substring check, which garbled-but-JSON-shaped garbage could still satisfy.
# Fixed (real JSON parse + per-outbound type/tag check), no user-visible
# behavior change. Cut as **build 126** (same 1.0.33 train), VALID in ASC,
# assigned to Internal Testers (verified via include=betaGroups). **1.0.33/126
# is now the build to test on-device** — 125 is superseded (same base, missing
# this fix; the poisoned cache on the device that hit this bug needs a
# reinstall or a clean-network refresh to actually clear, independent of the
# code fix).
#
# ⚠️ 2026-07-11 (still later same day): on-device test of 126 hit the ACTUAL
# root cause of "Нет конфигурации" — live-verified (curl) that NL
# (147.45.252.234) and SPB (185.218.0.43), both in `AppConfig.directBackendIPs`,
# answer `/api/v1/mobile/config` with a fast empty-body HTTP 400 (~0.3-0.4s).
# NL's backend was stopped by the 2026-06-29 WAW failover; SPB is separately
# confirmed application-dead (roadmap SPB-RECOVER, P0). The hedged race takes
# the FIRST leg to pass its win-check, not the best one, so these fast wrong
# legs could beat a slower-but-correct primary/decoy/MSK leg under the old
# `.anyBelow500` policy. Fixed: pruned NL+SPB from directBackendIPs (MSK-only,
# live-verified healthy), added `FallbackWinPolicy.definitiveConfigOnly`
# (mirrors GetConfig's real 2xx/401/403/404/409 surface) applied to
# `fetchConfig`, plus race.* outcome logging to TunnelFileLogger (was
# AppLogger-only, invisible in the app's own exported diagnostic log). Cut as
# **build 127** (same 1.0.33 train), VALID in ASC, assigned to Internal
# Testers (verified via include=betaGroups). **1.0.33/127 is now the build to
# test on-device** — 126 is superseded (its own config-cache-validation fix is
# still included, just missing this one).
#
# ⚠️ 2026-07-12: the new race telemetry from 127 (finally!) showed the race
# DID succeed on the real device — decoy leg (SPB), real HTTP 200 in
# ~250-600ms — yet the config still never cached and connect still failed.
# One layer deeper: nginx serves `/api/v1/mobile/config` with
# `Transfer-Encoding: chunked` and no Content-Length (live-verified,
# curl -D-), and `DirectConnection` (the raw HTTP/1.1 client BOTH the decoy
# and direct-IP race legs use — URLSession can't override SNI for a resolved
# IP) never decoded chunked bodies: it took every byte after the header
# terminator verbatim, splicing literal hex chunk-size lines into the JSON —
# exactly the two `decode config` errors seen on-device the whole time
# ("invalid character 'd'..." from an a-f hex chunk-size, "cannot unmarshal
# number..." from an all-decimal one). Primary (URLSession) was never
# affected — Foundation de-chunks transparently. Fixed: added
# `DirectConnection.dechunk(_:)`, applied on `Transfer-Encoding: chunked`;
# pinned with unit tests. Cut as **build 128** (same 1.0.33 train), VALID in
# ASC, assigned to Internal Testers (verified via include=betaGroups).
# **1.0.33/128 is now the build to test on-device** — very likely THE actual
# fix for the whole night's "не подключилось" — 125/126/127 all superseded
# (their own fixes are independently correct and still included).
#
verified: 2026-07-11 (partial — see warning above; full ASC re-pull still owed)

app:
  bundle_id: com.madfrog.vpn
  apple_id: 6761008632
  team_id: 99W3C374T2
  developer_org: "ИП Tkachuk Maksim Nikolaevich"
  duns: "94245265"

asc_api:
  key_id: 6HX3DA4P2Y
  issuer_id: bcb0c156-fec7-4fcd-994e-fd7cc81b5242
  key_path: ~/private_keys/AuthKey_6HX3DA4P2Y.p8
  # JWT generation snippet: see playbooks/asc-api-checks.md
  # second ASC API key (also valid): TJ9MRF55JA (~/private_keys/AuthKey_TJ9MRF55JA.p8)

# APNs Auth Key for push notifications (SUPPORT-CHAT P4). Created 2026-06-04 in
# the Apple Developer portal (Keys → APNs). DIFFERENT from the ASC API key above.
apns:
  key_id: 67FDF5G68V
  team_id: 99W3C374T2
  topic: com.madfrog.vpn          # APNs topic = the iOS app bundle id
  key_path: ~/private_keys/AuthKey_67FDF5G68V.p8   # also in ~/.secrets.env as APNS_*
  environment: "Sandbox & Production"   # Team Scoped (All Topics); choice is irreversible
  # Used by the (to-build) Go APNs HTTP/2 sender — signs JWT with iss=team_id, kid=key_id.
  # NOT yet wired into the backend/iOS — that's the rest of P4 (device tokens, entitlement, sender).

# Live App Store version. `READY_FOR_SALE` = users can install via App Store.
versions:
  current:
    version: "1.0.32"    # ⚠️ corrected 2026-07-11 — was showing 1.0.28/91 (a full generation
    build: 113            # stale). See the file-level warning above for sourcing.
    appStoreState: READY_FOR_SALE     # per separate audit's direct ASC API check, 2026-07-11
    asc_version_id: NOT RE-FETCHED — was d9d28448-... for the OLD 1.0.28 entry, do not reuse
    asc_build_id: NOT RE-FETCHED — was 6a033dc3-... for the OLD 1.0.28 entry, do not reuse
    contents_note: |
      Corresponds to the AUTH-RKN-DIRECT-IP sign-in fix per project.yaml's 2026-06-29
      snapshot (submitted 2026-06-09). Exact `ships:` list not re-verified this session —
      cross-check project.yaml#product.app_store.in_review (2026-06-29 wording, itself
      describing this same 113 build before it went live) before trusting specifics below.
    known_issues:   # STALE — carried over from the old 1.0.28 entry, not re-verified for 113
      - "UI-FLAG-HOME (flag emoji fallback on home card) — likely fixed since 38cc596 (data-driven country flags), see project.yaml recent-log"
      - SETTINGS-L10N (AUTO-CONNECT section in English)

  # Latest TestFlight build. 1.0.30 (102) carries the macOS keychain re-sign
  # fix (KEYCHAIN-MACOS). Cut as a NEW train because 1.0.29 (101) is locked in
  # App Store review → Apple closed the 1.0.29 train (error 90186). 1.0.29's
  # review is untouched; 1.0.30 App Store submission is a separate later step.
  testflight:
    version: "1.0.31"
    build: 111                        # iOS; assigned to Internal Testers (108 superseded untested)
    processingState: PROCESSING       # uploaded 2026-06-04 ~19:10Z, VALID poll in flight
    build_uploaded: 2026-06-04T~19:10Z (iOS)
    ships:
      - STORE-COUNTRY (sends X-Store-Country — App Store download region capture)
      - INAPP-ANNOUNCEMENTS (b110: in-app announcement card on app open — centered, dismissible, CTA)
      - SUPPORT-CHAT-POLISH (b109: diag confirm → alert; widget welcome/day-sep/file-bubble; branded log filename)
      - SUPPORT-CHAT-LOGFIX (b107: diag "Отправить лог" — fresh token + real singbox.log attach)
      - (cumulative 1.0.31 train) support chat P2-P4, attachments, APNs push, widget-timer, FR-SELECT
    groups:
      ios:   {id: 19b338f9-82fa-4371-ae21-bea225d94076, hasAccessToAllBuilds: false, assigned: explicit}
      macos: {id: 32c7c210-fe28-4992-86b5-7be773667ef3, hasAccessToAllBuilds: true,  assigned: auto}
    note: |
      1.0.31 is the active TestFlight train (b103→107). macOS not re-cut for b107
      (this fix is iOS-relevant; the shared SupportChatView still compiles on macOS
      — verified). 1.0.30/102 (KEYCHAIN-MACOS) is the separate App Store submission
      (see in_review). iOS Internal Testers group needs EXPLICIT build assignment
      (POST /betaGroups/{id}/relationships/builds → 204; verify via include=betaGroups).
  prev_testflight:
    version: "1.0.29"
    build: 101                        # superseded by 102 on TestFlight; still the in_review build
    processingState: VALID            # verified live ASC API 2026-06-02
    build_uploaded: 2026-06-02T11:34:00Z
  in_review:
    version: "1.0.31"
    build: 112
    appStoreVersion_id: 830edfff-f1b2-4d4b-9c73-0493caf90d48
    build_id: d5a45335-f82e-4808-8ac0-648dd64057f4
    appStoreState: WAITING_FOR_REVIEW   # submitted to App Store 2026-06-04, AFTER_APPROVAL auto-release
    review_submission: 0c3c5752-c146-4344-b59b-edaf9ab5d736
    contents: support chat (P2-P4) + APNs push + in-app announcements + store-country + chat polish; in-app PRIVACY POLICY updated for 5.1.1 (chat/push/device-context disclosed)
    user_todo: ASC App Privacy nutrition label (web-only, no public API) — add chat/push/diagnostics data types before the reviewer picks it up
    note: |
      Internal-testing builds must be EXPLICITLY assigned to the "Internal Testers"
      beta group (hasAccessToAllBuilds=false) via ASC API or they don't appear in
      TestFlight — see playbooks/ios-cli-release.md.
    ships:
      - ACCT-IDENTITY (no anon-demote; Keychain identity; ReauthView recovery ladder)
      - UI-FLAG-HOME + vector CountryFlag (no emoji) on home & server list
      - SETTINGS-L10N (AUTO-CONNECT section localized EN+RU)
      - PRO badge opens paywall; two server-list refresh buttons merged into one
      - SEC-01 client behavior (server-side trial-once already deployed)
    pending_verification:
      - STOREKIT-VERIFY (non-CIS Apple ID → 4 products + Restore)
      - ACCT-IDENTITY re-auth ladder on device
    history: builds 93 (no L10N) · 94 (L10N) · 95 (flag fixes) · 96-97 (vector flags) · 98 (PRO+refresh)
    superseded_builds:
      - build 93 (uploaded 2026-06-01T00:26Z, VALID) — replaced before submission

  previous:
    version: "1.0.27"
    build: 90
    asc_version_id: 857045e1-2b09-4d20-9af1-95c9f28285bf
    appStoreState: superseded
    was_live: 2026-05-28..2026-05-29

# Non-renewing subscription IAPs. All 4 APPROVED 2026-05-31.
# StoreKit path unblocked for non-CIS users. RU/CIS users go via WebPaywallView
# (FreeKassa) — separate path, see roadmap PAY-RU-DIAG.
iaps:
  type: NON_RENEWING_SUBSCRIPTION   # no auto-renew, no Subscription Group
  state_verified: 2026-05-31T00:30:00Z   # all 4 APPROVED (live ASC API)
  availability: 175 territories incl. RU/CIS   # verified inAppPurchaseAvailabilities
  pricing: USA base $1.99 + 174 auto-converted territories
  products:
    - product_id: com.madfrog.vpn.sub.30days
      apple_id: 6762097906
      state: APPROVED
    - product_id: com.madfrog.vpn.sub.90days
      apple_id: 6762098097
      state: APPROVED
    - product_id: com.madfrog.vpn.sub.180days
      apple_id: 6762098056
      state: APPROVED
    - product_id: com.madfrog.vpn.sub.365days
      apple_id: 6762097872
      state: APPROVED
  notes: |
    First submission (2026-05-27) stalled behind the build-89 reject; re-submitted
    standalone 2026-05-28 after build 90 went live; all 4 APPROVED by 2026-05-31.
    NOTE: approval unblocks StoreKit (non-CIS) only. RU/CIS users never see these
    IAPs — PaywallRouter sends them to WebPaywallView (FreeKassa/СБП).

# Storefront routing decision lives in decisions/0003-storefront-routing.md.
# Short version: RU/CIS storefronts → WebPaywallView (FreeKassa/SBP),
# everyone else → StoreKit. Driven by Storefront.current.countryCode,
# not Locale.
storefront_routing:
  storekit_route: non_cis_storefronts
  web_paywall_route: [RUS, KAZ, BLR, UZB, UKR, AZE, ARM, GEO, KGZ, TJK, TKM, MDA]

# App Privacy data types declared in ASC.
# Verified via Chrome MCP 2026-05-28 (App Privacy section).
# Public ASC API has no endpoint for this — must check web UI.
privacy:
  declared_data_types:
    contact_info:
      - Email Address      # linked, app_functionality
      - Name               # linked, app_functionality
    identifiers:
      - User ID            # linked, app_functionality
      - Device ID          # linked, app_functionality
    purchases:
      - Purchase History   # linked, app_functionality
    usage_data:
      - Product Interaction   # linked, analytics — covers EventTracker paywall.* / app.*
    diagnostics:
      - Other Diagnostic Data # linked, analytics+app_functionality — covers EventTracker vpn.connect.* + crash reports via Sentry (EU region, no PII)
  tracking_disclosed: false              # we don't share with 3rd parties for tracking
  notes: |
    "Other Diagnostic Data" covers two things:
      1. EventTracker app/vpn telemetry (USR-09 Phase 2, shipped 1.0.27).
      2. Crash reports via Sentry — EU region (de.sentry.io), no PII,
         sendDefaultPii=false, beforeSend scrubs user / queryString /
         device.name / serverName. See decisions/0007 + LAUNCH-03 in
         roadmap.yaml#done["2026-05-28"].
    Apple App Privacy classifies "Crash logs and other technical
    information" under "Other Diagnostic Data", so no new declared
    data type was required when LAUNCH-03 shipped.
  crash_reporting:
    vendor: Sentry
    region: EU (de.sentry.io, Frankfurt)
    dsn_storage: Info.plist key SENTRY_DSN (build-time inject, never committed)
    pii_disabled: true                # sendDefaultPii=false + beforeSend scrub
    session_tracking: false           # enableAutoSessionTracking=false
    performance_tracing: false        # tracesSampleRate=0
    scope: [MadFrogVPN (iOS), MadFrogVPNMac]   # NOT PacketTunnel (NE memory limits)
    reasoning_doc: decisions/0007-sentry-eu-crash-reporting.md
    shipped: 2026-05-28               # version target: build 91+
